services:
  - type: web
    name: api-prediccion-metano
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "uvicorn api:app --host 0.0.0.0 --port 10000"
    plan: free
    autoDeploy: true
    envVars:
      - key: PORT
        value: 10000
